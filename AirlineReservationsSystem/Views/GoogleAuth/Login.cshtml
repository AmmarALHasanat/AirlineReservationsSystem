@{
    ViewData["Title"] = "Login with Google";
}

<h2 class="text-center">@ViewData["Title"]</h2>

<p class="text-center">Log in to access your personalized settings and preferences.</p>

<div class="text-center">
    <a href="@Url.Action("Login", "GoogleAuth")" class="btn btn-danger">
        <i class="fab fa-google"></i> Login with Google
    </a>
</div>
